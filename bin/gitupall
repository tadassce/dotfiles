#! /usr/bin/env ruby

home = Dir.pwd

Dir['*']
  .select { |d| File.exist?("#{d}/.git") }
  .each do |dir|

  Dir.chdir dir

  name = dir.split('/').last
  emoji = '🐙'
  puts "#{emoji} Pulling #{name}"
  system 'git pull'

  Dir.chdir home
end
