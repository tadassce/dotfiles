#!/bin/zsh

host=`hostname`
time_now=`date +%H:%M`
# cal_next=`cal-next`
# wifi_name=`wifi-name`
# battery=`battery`

if [[ "${host:(-5):5}" == 'local' ]]; then
  echo $time_now
else
  echo "$host | $time_now"
fi
